'use strict';

/**
 * Annoying that this is called "index.js", when it's just a demo.
 * Alas, this is a requirement from create-react-app.
 **/

var React = require('react');
var ReactDOM = require('react-dom');
var Demo = require('./demo/demo');

ReactDOM.render(React.createElement(Demo, null), document.getElementById('root'));